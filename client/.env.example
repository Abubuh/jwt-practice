# Opcional: URL del API. En desarrollo, si no defines nada, se usa http://localhost:3000
# En producción (Vercel), puedes definir VITE_API_URL para apuntar a Railway sin depender del fallback del código.

VITE_API_URL=http://localhost:3000
